<div class="container">
  <nav>
    <% if logged_in? %>
    Logged in as: <%= current_user.email %> | 
    <a href="/logout">Logout</a>
    <% else %>
    <a href="/register">Register</a> |
    <a href="/login">Login</a>
    <% end %>
  </nav>
</div>
